<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html

    Header always set X-Content-Type-Options "nosniff"

    # API PHP
    Alias /api /var/www/app/api

    <Directory /var/www/app/api>
        Options FollowSymLinks
        AllowOverride None
        Require all granted
    </Directory>

    # Frontend React
    <Directory /var/www/html>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted

        RewriteEngine On

        # No enviar las llamadas /api a React Router
        RewriteCond %{REQUEST_URI} !^/api/

        # Si el recurso solicitado existe físicamente, Apache lo sirve normalmente
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d

        # Si no existe, React Router resuelve la ruta
        RewriteRule ^ /index.html [L]
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>